#!/bin/sh
source "$HOME/.dotfiles/macos-aliases.sh"
source "$HOME/.dotfiles/macos-defaults.sh"

# This only needs to be run once; otherwise, it slows down terminal windows
# source $HOME/.dotfiles/macos-setdefaults.sh 
