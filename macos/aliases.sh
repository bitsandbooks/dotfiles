#!/usr/bin/env sh
alias alint=ansible-lint
alias aplay=ansible-playbook
alias brewup="brew bundle --file $DOTFILES/macos/Brewfile && brew update && brew upgrade && brew cleanup"
alias o=open
alias oo='open .'
alias ls='ls -H'
